# Inlined into the client bundle at build time by `next build`.
# Public by design — it is only the API's address, never a credential.
NEXT_PUBLIC_API_BASE=https://morning-brief-api.abdullah-alshoshan.workers.dev
